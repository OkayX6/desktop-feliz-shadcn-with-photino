{
    "private": true,
    "type": "module",
    "scripts": {
        "prestart": "dotnet tool restore && dotnet fable clean --yes",
        "start": "concurrently --kill-others-on-fail -c red,blue 'npm:start:desktop' 'npm:start:ui'",
        "start:desktop": "dotnet watch run --project ./Desktop",
        "start:ui": "dotnet fable watch ./UserInterface --runFast vite",
        "prebuild": "dotnet tool restore && dotnet fable clean --yes",
        "build": "npm run build:ui && npm run build:desktop",
        "build:ui": "dotnet tool restore && dotnet fable ./UserInterface --run vite build",
        "build:desktop": "dotnet build -c Release ./Desktop",
        "pretest": "dotnet tool restore && dotnet fable clean --yes",
        "test:live": "cd UserInterface.Tests && dotnet fable watch --run npx vite",
        "test": "dotnet fable ./UserInterface.Tests --outDir dist/tests && mocha dist/tests -r esm"
    },
    "dependencies": {
        "@radix-ui/react-slot": "^1.2.3",
        "@tailwindcss/vite": "^4.1.11",
        "class-variance-authority": "^0.7.1",
        "clsx": "^2.1.1",
        "date-fns": "^2.30.0",
        "lucide-react": "^0.541.0",
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "tailwind-merge": "^3.3.1",
        "tailwindcss": "^4.1.11"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^4.2.1",
        "concurrently": "^9.2.1",
        "esm": "^3.2.25",
        "mocha": "^11.5.0",
        "tw-animate-css": "^1.3.6",
        "vite": "^5.1.4"
    }
}
